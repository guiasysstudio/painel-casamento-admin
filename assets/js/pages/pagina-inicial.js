import { bootstrapPage, db, $, toast } from "../admin-core.js";
import { doc,getDoc,setDoc,Timestamp,serverTimestamp } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
function localInput(value){if(!value)return'';const d=value.toDate?value.toDate():new Date(value),p=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`;}
async function load(){const s=await getDoc(doc(db,'configuracoes','publico'));if(!s.exists())return;const c=s.data();$("siteName").value=c.siteName||'';$("pageTitle").value=c.pageTitle||'';$("introText").value=c.introText||'';$("weddingDate").value=localInput(c.weddingDate);$("confirmationDeadline").value=localInput(c.confirmationDeadline);$("venueName").value=c.venueName||'';$("venueAddress").value=c.venueAddress||'';$("mapsUrl").value=c.mapsUrl||'';$("childMaxAge").value=c.childMaxAge??12;$("reservationHours").value=c.reservationHours??24;}
bootstrapPage({permission:'configuracoes',onReady:async()=>{await load();$("siteConfigForm").onsubmit=async e=>{e.preventDefault();await setDoc(doc(db,'configuracoes','publico'),{siteName:$("siteName").value.trim(),pageTitle:$("pageTitle").value.trim(),introText:$("introText").value.trim(),weddingDate:Timestamp.fromDate(new Date($("weddingDate").value)),confirmationDeadline:Timestamp.fromDate(new Date($("confirmationDeadline").value)),venueName:$("venueName").value.trim(),venueAddress:$("venueAddress").value.trim(),mapsUrl:$("mapsUrl").value.trim(),childMaxAge:Number($("childMaxAge").value),reservationHours:Number($("reservationHours").value),updatedAt:serverTimestamp()},{merge:true});toast('Página inicial salva');};}});
